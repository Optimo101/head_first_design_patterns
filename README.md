# Head First Design Patterns

A collection of minimal, runnable TypeScript examples of classic software design patterns from *Head First Design Patterns*.

Each pattern lives in its own folder under `/patterns` and is self-contained.

---

## Running a Pattern

From the repository root, run a single pattern with:

```bash
npm run <pattern-name>
